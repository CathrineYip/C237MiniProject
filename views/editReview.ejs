<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Parentica!</title>
        <style>
          body {
              display: flex;
              flex-direction: column;
          }
          .sidebar {
              width: 200px;
              float:left;
              position: fixed;
              top: 0;
              left: 0;
              height: 100%;
              background-color: #f8f9fa;
              padding: 10px;
              border-right: 1px solid #dee2e6;
          }
          .sidebar input {
              margin-bottom: 15px;
              width: 100%;
              padding: 5px;
              border: 1px solid #dee2e6;
              border-radius: 2px;
          }
          .sidebar nav a {
              display: block;
              margin-bottom: 10px;
              color: #343a40;
              text-decoration: none;
          }
          .sidebar nav a:hover {
              background-color: #e9ecef;
              border-radius: 4px;
              padding-left: 10px;
          }
          .main-content {
              margin-left: 250px;
              flex: 1;
              padding: 20px;
          }
          header {
              display: flex;
              justify-content: space-between;
              align-items: center;
              border-bottom: 1px solid #dee2e6;
              padding-bottom: 10px;
              margin-bottom: 20px;
          }
          .notifications {
              display: flex;
              align-items: center;
          }
          .notifications .bell {
              font-size: 1.5rem;
              margin-right: 10px;
          }
          .tabs {
           display: flex;
           border-bottom: 2px solid rgb(0, 110, 255);
          }
  
          .label1 {
            font-size: x-large;
            cursor: pointer;
            background-color: white;
            margin-right: 5px;
            transition: background-color 0.3s ease, color 0.3s ease;
          }
  
          .label1:hover {
            background-color: #e9ecefb5; 
          }
         h2 {
              font-family: 'Georgia', sans-serif;
              font-size: 2.5rem;
              color: #002fff;
              text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
              margin: 20px 0;
              padding-bottom: 10px;
              border-bottom: 2px solid #008cff;
              text-transform: capitalize;
          }
      </style>
    </head>
    
<body>
  <div class="sidebar">
    <input type="text" placeholder="Search...">
    <nav>
        <a href="/">Home</a>
        <a href="/addReviewsForm">Add a review</a>
    </nav>
</div>
  <div class="main-content">
    <div class="container">
        <br>
        <div class="text-center"><h2>Update Review</h2></div>   
        <br>
        <div class="text-center">
          <% if (reviews) { %> 
            <form action="/editReview/<%= reviews.reviewID %>" method="POST"  enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="username" class="form-label">Username:</label>
                    <input type="text" id="username" name="username" class="form-control" value="<%= reviews.username %>" required>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description:</label>
                    <textarea id="description" name="description" class="form-control" required><%= reviews.description %></textarea>
                </div>
                <div class="mb-3">
                  <label for="image">Image:</label>
                  <input type="text" name="image" value=<%=reviews.image%> disabled><br>
                  <img src="/images/<%=reviews.image%>"width="20%"><br><br>
                  New Image: <br> <input type="file" id="image" name="image" accept="image/*" value=<%=reviews.image%>><br><br>
                </div>
                <button type="submit" class="btn btn-primary">Update Review</button>
            </form>
          <% } else { %>
              <p>No review found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
  </div>
</body>
</html>
